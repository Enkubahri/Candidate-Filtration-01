# ===============================================================
# VERCEL DEPLOYMENT REQUIREMENTS
# ===============================================================
# Minimal requirements for standalone.py - Vercel serverless deployment

# Core Flask Framework (Only what's needed)
Flask==2.3.3

# Note: 
# - No flask-cors (we use manual CORS headers in standalone.py)
# - No other dependencies to avoid build timeouts
# - Standalone.py is designed to work with just Flask
