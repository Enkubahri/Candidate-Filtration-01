# Environment Configuration for Candidate Filtration System API

# Flask Configuration
FLASK_ENV=development
SECRET_KEY=your-very-secret-production-key-here
DEBUG=false

# Server Configuration
HOST=0.0.0.0
PORT=5000

# Database Configuration
DATABASE_URL=candidates.db

# File Upload Configuration
UPLOAD_FOLDER=uploads
MAX_CONTENT_LENGTH=16777216

# Email Configuration (if using email features)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=your-email@domain.com
MAIL_PASSWORD=your-app-password

# Production Settings
# For production deployment, copy this file to .env and update values
# Make sure to use strong, unique values for SECRET_KEY
